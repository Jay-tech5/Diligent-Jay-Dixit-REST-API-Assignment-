# Agent Guidelines & Rules

## Git Workflow
- Automatically commit and push code changes to GitHub (`git add . && git commit -m "..." && git push origin main`) whenever code modifications or features are completed, without waiting for explicit prompt instructions.
